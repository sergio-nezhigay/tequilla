{{ 'our-promise.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-our-promise{
      background-color: {{ section.settings.background_color }};
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  .container-our-promise{
      background-image: url({{ section.settings.main_banner | image_url }});
  }
  @media (max-width: 767px){
      .section-our-promise{
          padding-top: {{ section.settings.padding_top_mob }}px;
          padding-bottom: {{ section.settings.padding_bottom_mob }}px;
      }
  }
{%- endstyle -%}
<section class="section-our-promise">
  <div class="container-our-promise">
    {% if section.settings.main_img != blank %}
      {{
        section.settings.main_img
        | image_url: width: section.settings.main_img.width
        | image_tag: class: 'img-our-promise'
      }}
    {% endif %}
    {% if section.settings.title != blank %}
      <div class="title-our-promise">{{ section.settings.title }}</div>
    {% endif %}
    {% if section.settings.description != blank %}
      <div class="description-our-promise">{{ section.settings.description }}</div>
    {% endif %}
  </div>
</section>
{% schema %}
{
  "name": "Our promise",
  "settings": [
    {
      "type": "image_picker",
      "id": "main_banner",
      "label": "Image banner"
    },
    {
      "type": "image_picker",
      "id": "main_img",
      "label": "Image"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background"
    },
    {
      "type": "richtext",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Desktop"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding top",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding bottom",
      "default": 0
    },
    {
      "type": "header",
      "content": "Mobile"
    },
    {
      "type": "range",
      "id": "padding_top_mob",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding top",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mob",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding bottom",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Our promise"
    }
  ]
}
{% endschema %}
